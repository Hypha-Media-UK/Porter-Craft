<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Porter Management | Craft CMS</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --primary: #2563eb;
            --primary-hover: #1d4ed8;
            --secondary: #64748b;
            --success: #10b981;
            --danger: #ef4444;
            --warning: #f59e0b;
            --light: #f8fafc;
            --dark: #1e293b;
            --border: #e2e8f0;
            --background: #ffffff;
            --card-bg: #f8fafc;
            --text: #334155;
            --text-light: #64748b;
            --shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
            --radius: 8px;
            --transition: all 0.2s ease-in-out;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: var(--text);
            background-color: var(--light);
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        h1, h2, h3 {
            color: var(--dark);
            font-weight: 600;
            line-height: 1.3;
        }

        h1 {
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid var(--border);
        }

        h2 {
            font-size: 1.4rem;
            margin: 1.5rem 0 1rem;
        }

        h3 {
            font-size: 1.2rem;
            margin: 1.25rem 0 0.75rem;
        }

        /* Tables */
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 1.25rem 0;
            box-shadow: var(--shadow);
            border-radius: var(--radius);
            overflow: hidden;
            background-color: var(--background);
        }

        table, th, td {
            border: none;
        }

        th, td {
            padding: 0.75rem 1rem;
            text-align: left;
        }

        th {
            background-color: var(--primary);
            color: white;
            font-weight: 500;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 0.5px;
        }

        tbody tr {
            border-bottom: 1px solid var(--border);
        }

        tbody tr:last-child {
            border-bottom: none;
        }

        tbody tr:hover {
            background-color: rgba(0, 0, 0, 0.02);
        }

        /* Cards and Containers */
        .card, .settings-section, .add-porter-form, .add-supervisor-form, .notification {
            background-color: var(--background);
            border-radius: var(--radius);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow);
        }

        .porters-list, .add-porter-form, .supervisors-list, .add-supervisor-form {
            margin-bottom: 2rem;
        }

        /* Form Elements */
        .field {
            margin-bottom: 1.25rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--dark);
        }

        input[type="text"], select, textarea {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid var(--border);
            border-radius: calc(var(--radius) / 2);
            font-size: 1rem;
            transition: var(--transition);
            background-color: var(--light);
        }

        input[type="text"]:focus, select:focus, textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.2);
        }

        /* Buttons */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 0.625rem 1.25rem;
            font-size: 0.9rem;
            font-weight: 500;
            cursor: pointer;
            border-radius: calc(var(--radius) / 2);
            text-decoration: none;
            transition: var(--transition);
            gap: 0.5rem;
        }

        .btn:hover {
            background-color: var(--primary-hover);
            transform: translateY(-1px);
        }

        .btn.small {
            padding: 0.375rem 0.75rem;
            font-size: 0.8rem;
        }

        .btn.secondary {
            background-color: var(--secondary);
        }

        .btn.secondary:hover {
            background-color: #4b5563;
        }

        .btn.success {
            background-color: var(--success);
        }

        .btn.success:hover {
            background-color: #059669;
        }

        .btn.danger {
            background-color: var(--danger);
        }

        .btn.danger:hover {
            background-color: #dc2626;
        }

        /* Notifications */
        .notification {
            padding: 1rem 1.25rem;
            margin-bottom: 1.5rem;
            border-radius: var(--radius);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .notification.success {
            background-color: #ecfdf5;
            border-left: 4px solid var(--success);
            color: #064e3b;
        }

        .notification.error {
            background-color: #fef2f2;
            border-left: 4px solid var(--danger);
            color: #7f1d1d;
            font-size: 0.9rem;
            margin-top: 0.5rem;
            display: flex;
        }

        .notification.warning {
            background-color: #fffbeb;
            border-left: 4px solid var(--warning);
            color: #92400e;
        }

        .field .error {
            color: var(--danger);
            font-size: 0.85rem;
            margin-top: 0.4rem;
            display: block;
        }

        /* Header with Navigation */
        .header-with-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .header-with-nav h1 {
            margin-bottom: 0;
            border-bottom: none;
            padding-bottom: 0;
        }

        .nav-links {
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }

            .header-with-nav {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .nav-links {
                margin-top: 0.5rem;
            }

            th, td {
                padding: 0.5rem;
            }
        }

        @media (max-width: 576px) {
            .header-with-nav {
                margin-bottom: 1rem;
            }

            table {
                display: block;
                overflow-x: auto;
            }
        }
    </style>
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
</head>
<body>
    <header class="site-header">
        <div class="header-content">
            <div class="logo">
                <i class="bi bi-buildings-fill"></i>
                <span>Porter Management</span>
            </div>
            <nav class="main-nav">
                <a href="{{ siteUrl() }}" class="nav-item {% if craft.app.request.getSegment(1) == '' %}active{% endif %}">
                    <i class="bi bi-house-door"></i> Dashboard
                </a>
                <a href="{{ siteUrl('settings') }}" class="nav-item {% if craft.app.request.getSegment(1) == 'settings' %}active{% endif %}">
                    <i class="bi bi-gear"></i> Settings
                </a>
            </nav>
        </div>
    </header>

    <main class="container">
        {% block content %}{% endblock %}
    </main>

    <footer class="site-footer">
        <div class="footer-content">
            <p>© {{ now|date('Y') }} Porter Management System | Powered by Craft CMS</p>
        </div>
    </footer>

    <style>
        /* Header Styles */
        .site-header {
            background-color: var(--dark);
            color: white;
            margin-bottom: 2rem;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.5rem;
        }

        .logo {
            display: flex;
            align-items: center;
            font-weight: 600;
            font-size: 1.2rem;
            gap: 0.5rem;
        }

        .logo i {
            font-size: 1.4rem;
            color: var(--primary);
        }

        .main-nav {
            display: flex;
            gap: 1rem;
        }

        .nav-item {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            padding: 0.5rem 0.75rem;
            border-radius: var(--radius);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: var(--transition);
        }

        .nav-item:hover {
            color: white;
            background-color: rgba(255, 255, 255, 0.1);
        }

        .nav-item.active {
            color: white;
            background-color: var(--primary);
        }

        /* Footer Styles */
        .site-footer {
            margin-top: 3rem;
            color: var(--text-light);
            font-size: 0.85rem;
            text-align: center;
            padding: 1.5rem 0;
            border-top: 1px solid var(--border);
        }

        /* Responsive header */
        @media (max-width: 576px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
                padding: 1rem;
            }

            .main-nav {
                width: 100%;
                justify-content: space-around;
            }
        }
    </style>
</body>
</html>
